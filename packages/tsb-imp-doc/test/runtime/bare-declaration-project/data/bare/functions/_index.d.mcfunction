#> bare:_index.d
#
# The author scoped this header with @within, but the directives below own no
# IMP-Doc block, so the scope never reached them.
#
# @within function bare:**

#declare tag BareTag
#define storage bare:store

#> Scoped
# @private
#declare tag ScopedTag
