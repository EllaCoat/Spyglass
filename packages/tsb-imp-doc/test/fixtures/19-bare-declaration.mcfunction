#> fixture:_index.d
#
# The directives below are separated from this header by a blank line, so they
# own no IMP-Doc block. v3 parsed them through the CommandTree `comments`
# section, which registered them regardless of any surrounding doc comment.
#
# @within function fixture:**

#declare tag BareTag
#define storage fixture:bare

#> Scoped
# @public
#declare tag ScopedTag
